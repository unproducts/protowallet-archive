export const recordTypes = ['Expense', 'Income', 'Transfer'];
export const recurringTypes = ['count', 'date'];
const colors = ['coral', 'darkorange', 'gold', 'lightpink', 'royalblue', 'teal'];
export const colorOptions = colors.map((color) => ({ value: color, label: color }));